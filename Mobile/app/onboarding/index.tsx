import { OnboardingNavigator } from '@/features/onboarding/OnboardingNavigator';
import React from 'react';

/**
 * Onboarding entry route for expo-router.
 * Maps to: /onboarding
 */
export default function OnboardingPage() {
  return <OnboardingNavigator />;
}
